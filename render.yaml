services:
  - type: web
    name: medical-reference-app
    env: python
    buildCommand: bash minimal_build.sh
    startCommand: gunicorn minimal_app:app
    envVars:
      - key: FLASK_ENV
        value: production
    healthCheckPath: /health
    autoDeploy: true
